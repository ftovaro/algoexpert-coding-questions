const optimal_freelancing = require('./index');

test('test #1', () => {
  expect(optimal_freelancing()).toEqual(null);
});

test('test #2', () => {
  expect(optimal_freelancing()).toEqual(null);
});
