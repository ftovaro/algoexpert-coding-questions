const evaluate_expression_tree = require('./index');

test('test #1', () => {
  expect(evaluate_expression_tree()).toEqual(null);
});

test('test #2', () => {
  expect(evaluate_expression_tree()).toEqual(null);
});
