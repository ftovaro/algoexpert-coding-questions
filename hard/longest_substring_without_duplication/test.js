const longest_substring_without_duplication = require('./index');

test('test #1', () => {
  expect(longest_substring_without_duplication()).toEqual(null);
});

test('test #2', () => {
  expect(longest_substring_without_duplication()).toEqual(null);
});
