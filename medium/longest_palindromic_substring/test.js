const longest_palindromic_substring = require('./index');

test('test #1', () => {
  expect(longest_palindromic_substring()).toEqual(null);
});

test('test #2', () => {
  expect(longest_palindromic_substring()).toEqual(null);
});
